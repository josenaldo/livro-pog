# Agent entrypoint

This repo uses a single source of truth for agent configuration:

- Skills: `.github/skills/`
- Rules/instructions: `.github/instructions/`
- Prompt templates: `.github/prompts/`

If your agent/tooling supports custom instructions, read `.github/instructions/README.md` first.
